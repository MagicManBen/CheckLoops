// Reset welcome tour for testing - run this in browser console on staff.html
// This will clear the localStorage flag so the tour shows again

console.log('Clearing welcome tour localStorage flag...');
localStorage.removeItem('cl_welcome_tour_seen_v1');
console.log('✅ Welcome tour localStorage flag cleared!');
console.log('Refresh the page to see the tour.');

// Optional: Force refresh the page
// window.location.reload();