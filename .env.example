## Copy this file to .env and fill in the values, or set the env vars in your shell.
# Never commit your real service role key to version control.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=service_role_key_here
BASE_URL=http://localhost:5173

# Supabase credentials (anon key is safe for frontend use)
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Test credentials (for development only)
TEST_USER=you@example.com
TEST_PASS=your_password

# OpenAI API Key - NEVER commit this to version control
# Store this in Supabase Edge Function environment variables instead
# OPENAI_API_KEY=your_openai_api_key_here

# Optional: scope queries to a specific site
# SITE_ID=2

# SMTP Configuration for production email delivery
# Choose one of the configurations below and uncomment it

# === GMAIL SMTP (Recommended for personal/small business) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password  # Use App Password, not regular password
# SMTP_ADMIN_EMAIL=your-email@gmail.com

# === SENDGRID (Recommended for high volume) ===
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_ADMIN_EMAIL=your-admin@yourdomain.com

# === OUTLOOK/HOTMAIL ===
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password
# SMTP_ADMIN_EMAIL=your-email@outlook.com

# === CUSTOM SMTP SERVER ===
# SMTP_HOST=your-smtp-server.com
# SMTP_PORT=587
# SMTP_USER=your-username
# SMTP_PASS=your-password
# SMTP_ADMIN_EMAIL=admin@yourdomain.com

